# Installation

```
pip install gepa
```

# Run

Before you run this script, you should specifiy several keys (e.g. wandb and huggingface) along with the cache directory if needed
```
bash scripts/gepa-run.sh
```